services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: woodwise
      POSTGRES_USER: woodwise
      POSTGRES_PASSWORD: woodwise
    ports:
      - "5432:5432"
    volumes:
      - woodwise-postgres:/var/lib/postgresql/data

volumes:
  woodwise-postgres:
